#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(git rev-parse --show-toplevel)"

if ! command -v python3 >/dev/null 2>&1; then
  echo "python3 is required for secret scan." >&2
  exit 1
fi

python3 "$ROOT_DIR/scripts/security/scan_secrets.py" --mode staged
